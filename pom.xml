<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.sonatype.oss</groupId>
    <artifactId>oss-parent</artifactId>
    <version>7</version>
  </parent>
  <groupId>net.md-5</groupId>
  <artifactId>ForgeMod</artifactId>
  <version>1.5-7.7.0.566-R1</version> <!-- ${minecraft.version}-${forge.version}-${forgemodparent.revision} -->
  <name>ForgeMod</name>
  <packaging>pom</packaging>
  <url>https://github.com/agaricusb/ForgeMod</url>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <minecraft.version>1.5</minecraft.version>
    <minecraft_version>1_5</minecraft_version>
    <forge.majorversion>7</forge.majorversion>
    <forge.minorversion>7</forge.minorversion>
    <forge.revisionversion>0</forge.revisionversion>
    <forge.build>566</forge.build>
    <forge.mainversion>${forge.majorversion}.${forge.minorversion}.${forge.revisionversion}</forge.mainversion>
    <forge.version>${forge.mainversion}.${forge.build}</forge.version>
    <forgemodparent.revision>R1</forgemodparent.revision> <!-- increment when this parent pom is updated -->
    <fml.at>https://raw.github.com/MinecraftForge/FML/master/common/fml_at.cfg</fml.at>
    <forge.at>https://raw.github.com/MinecraftForge/MinecraftForge/master/common/forge_at.cfg</forge.at>
    <mcp.mappings>https://raw.github.com/MinecraftForge/FML/master/conf/</mcp.mappings>
    <mod.at></mod.at> <!-- optional access transformer if required by the mod -->
    <forge.suffix></forge.suffix> <!-- set for modified versions of forge (coremods) -->
  </properties>
  <profiles>
   <profile>
    <activation>
        <activeByDefault>true</activeByDefault>
    </activation>
    <id>built</id>
    <dependencies>
      <!-- after the remapped artifact is built, add it a dependency -->
      <dependency>
        <groupId>net.minecraftforge</groupId>
        <artifactId>minecraft-forge</artifactId>
        <version>${forge.version}-${forgemodparent.revision}${forge.suffix}</version>
        <type>jar</type>
        <scope>compile</scope>
      </dependency>
    </dependencies>
   </profile>
  </profiles>
  <dependencies>
    <dependency>
        <groupId>org.ow2.asm</groupId>
        <artifactId>asm-all</artifactId>
        <version>4.1</version>
    </dependency>
    <dependency>
        <groupId>com.google.guava</groupId>
        <artifactId>guava</artifactId>
        <version>14.0-rc3</version>
    </dependency>
    <dependency>
        <groupId>org.lwjgl.lwjgl</groupId>
        <artifactId>lwjgl</artifactId>
        <version>2.8.0</version>
    </dependency>
    <dependency>
        <groupId>org.lwjgl.lwjgl</groupId>
        <artifactId>lwjgl_util</artifactId>
        <version>2.8.4</version>
    </dependency>
    <dependency>
        <groupId>net.sourceforge.argo</groupId>
        <artifactId>argo</artifactId>
        <version>3.2</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.0</version>
        <configuration>
          <source>1.6</source>
          <target>1.6</target>
        </configuration>
      </plugin>
      <plugin>
            <groupId>net.md-5</groupId>
            <artifactId>specialsource-maven-plugin</artifactId>
            <version>1.1-SNAPSHOT</version>
            <executions>
                <execution>
                    <id>install-remapped-forge</id>
                    <phase>initialize</phase>
                    <goals>
                        <goal>install-remapped-file</goal>
                    </goals>
                    <configuration>
                        <groupId>net.minecraftforge</groupId>
                        <artifactId>minecraft-forge</artifactId>
                        <version>${forge.version}-${forgemodparent.revision}${forge.suffix}</version>
                        <inJars>
                            <param>http://assets.minecraft.net/${minecraft_version}/minecraft_server.jar</param>
                            <param>http://assets.minecraft.net/${minecraft_version}/minecraft.jar</param>
                            <param>http://files.minecraftforge.net/minecraftforge/minecraftforge-universal-${minecraft.version}-${forge.version}.zip</param>
                        </inJars>
                        <srgIn>${mcp.mappings}</srgIn>
                        <accessTransformers>
                            <param>${fml.at}</param>
                            <param>${forge.at}</param>
                            <param>pattern:*-synthetic **</param> <!-- magic -->
                            <param>${mod.at}</param> <!-- optional AT from mod itself -->
                        </accessTransformers>
                    </configuration>
                </execution>
                <execution>
                    <id>reobfuscate</id>
                    <phase>package</phase>
                    <goals>
                        <goal>remap</goal>
                    </goals>
                    <configuration>
                        <srgIn>${mcp.mappings}</srgIn>
                        <numeric>true</numeric>
                        <remappedDependencies>
                            <param>net.minecraftforge:minecraft-forge:${forge.version}-${forgemodparent.revision}${forge.suffix}:jar</param>
                        </remappedDependencies>
                    </configuration>
                </execution>
            </executions>
        </plugin>
    </plugins>
  </build>
</project>
